# Prettier covers the engine only. The documentation is hand-wrapped, and
# demos/, verification/ and tools/ are zero-dependency plain JavaScript that
# predates this config and keeps its own layout.
*.md
.github/
docs/
demos/
verification/
tools/
site/
_site/
dist/
dist-types/
node_modules/
package-lock.json

# The two block manifests are written by tools/make-textures.ts and
# tools/bake-textures.ts. Both already indent what they write, and prettier
# rewraps their arrays differently -- so a bake left a dirty tree behind every
# time and the diff said the pictures had changed when only the whitespace had.
assets/blocks/blocks.json
packages/client/public/blocks/blocks.json
