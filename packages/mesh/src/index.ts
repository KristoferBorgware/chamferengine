// Surface extraction, hexagon geometry and ambient occlusion. This package
// holds no WebGPU type: it writes through a sink it declares itself, so it runs
// under plain Node with no GPU present.
//
// Project 6 fills it in.
export const MESH_PLACEHOLDER = true;
